React Developer Tools est une extension officielle de React pour les navigateurs (Chrome, Edge, Firefox). Elle sert à analyser, déboguer et optimiser les applications React. Voici ses principales utilités :

🔍 Inspection des composants

Permet de visualiser la structure complète de ton application React (l’arborescence des composants).

Tu peux voir les props, state, et hooks de chaque composant en temps réel.

⚙️ Analyse du comportement

Montre comment les composants se mettent à jour lors d’interactions ou de changements de données.

Tu peux vérifier si un composant se re-render inutilement.

🎨 Aide au style et à la hiérarchie

Permet de repérer facilement les relations entre composants parents/enfants et de comprendre la logique de rendu.

Tu peux aussi voir le nom exact du composant React lié à un élément HTML dans le DOM.

🧭 Navigation rapide

Tu peux cliquer sur un composant dans l’interface et être redirigé automatiquement vers son code source dans ton éditeur (avec l’intégration VS Code).

⚡ Outil de performance

Le Profiler intégré aide à mesurer le temps de rendu des composants, ce qui est essentiel pour optimiser les performances.
